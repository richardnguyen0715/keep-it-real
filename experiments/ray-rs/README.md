# Ray Rust example

This small Rust program demonstrates sequential vs parallel execution using the `rayon` crate. It runs 4 tasks that each sleep for 1 second and compute a square.

Run:

```bash
cd experiments/ray-rs
cargo run --release
```
